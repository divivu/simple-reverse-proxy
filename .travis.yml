language: node_js
node_js:
  - 13

services:
  - docker

# Pre-testing installs
install:
  - echo "nothing needs to be installed"

# Scripts to be run such as tests
before_script:
  - echo "no tests"

script:
  - docker --version # print the version for logging
  - docker build -t simple-reverse-proxy .
  - docker tag simple-reverse-proxy divdocker789/simple-reverse-proxy:latest

after_success:
  - echo "7cgvacbcb9" | docker login -u "divdocker789" --password-stdin
  - docker push divdocker789/simple-reverse-proxy

